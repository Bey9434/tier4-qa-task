# .env が持つ変数の一覧。値はすべて公開デモ環境の既定値で機密ではない。
# 復号鍵（.env.keys）が無い環境では、このファイルを .env にコピーすれば平文のまま実行できる。
# その場合、平文で上書きした .env はコミットしないこと（リポジトリの .env は暗号化状態が正）
BASE_URL="http://localhost:4200"
API_URL="http://localhost:8091"
